# Runtime State - Workspace Tabs 与 Screen 级 Keep-Alive

**Last updated:** 2026-05-27 15:32
**Coordinator:** codex（项目经理 / 首席工程师）

## Task Status

| Task    | Status  | Started          | Completed        | Gate                              | Log                   |
| ------- | ------- | ---------------- | ---------------- | --------------------------------- | --------------------- |
| Task 01 | done    | 2026-05-26 10:05 | 2026-05-26 12:45 | codex review 通过                 | [log](task-01-log.md) |
| Task 02 | done    | 2026-05-26 11:20 | 2026-05-26 12:25 | codex review 通过                 | [log](task-02-log.md) |
| Task 03 | done    | 2026-05-26 12:45 | 2026-05-26 11:18 | codex review 通过                 | [log](task-03-log.md) |
| Task 04 | done    | 2026-05-26 12:45 | 2026-05-26 12:05 | codex review 通过                 | [log](task-04-log.md) |
| Task 05 | review  | 2026-05-26 12:05 | -                | codex review changes requested    | [log](task-05-log.md) |
| Task 06 | done    | 2026-05-26 14:08 | 2026-05-26 14:54 | codex verification 通过           | [log](task-06-log.md) |
| Task 07 | done    | 2026-05-26 14:08 | 2026-05-26 14:32 | codex verification 通过           | [log](task-07-log.md) |
| Task 08 | blocked | 2026-05-26 14:56 | -                | superseded by approved `spec-v2/` | [log](task-08-log.md) |

Status values: `pending` | `running` | `review` | `done` | `blocked`

## V2 Execution Status

| Task        | Status | Started    | Completed  | Gate                                            | Log                       |
| ----------- | ------ | ---------- | ---------- | ----------------------------------------------- | ------------------------- |
| Task V2-01  | done   | 2026-05-26 | 2026-05-27 | coordinator review 通过                         | [log](task-v2-01-log.md)  |
| Task V2-02A | done   | 2026-05-27 | 2026-05-27 | codex 五轮验收通过                              | [log](task-v2-02a-log.md) |
| Task V2-02B | done   | 2026-05-27 | 2026-05-27 | codex 三轮验收通过                              | [log](task-v2-02b-log.md) |
| Task V2-03  | done   | 2026-05-27 | 2026-05-27 | codex 二轮验收通过                              | [log](task-v2-03-log.md)  |
| Task V2-04  | done   | 2026-05-27 | 2026-05-27 | coordinator final review + full regression 通过 | [log](task-v2-04-log.md)  |

## Active Agents

| Agent | Task                                           | Since      |
| ----- | ---------------------------------------------- | ---------- |
| `%15` | product detail dirty-form keepAlive 收口已关闭 | 2026-05-27 |
| `%16` | keepAlive opt-out / closeGuard 契约复核已关闭  | 2026-05-27 |

## Replan Log

- 2026-05-26: 新建 folder plan，替代 `2026-05-25` 单文件计划。
- 2026-05-26: Task 05 首轮 review 未通过，阻塞点集中在 Activity 语义、Viewport props contract、真实集成测试覆盖，以及 keep-alive 首次切换空白窗口。
- 2026-05-26 14:08: 已重新分配并启动 Task 06（`%11`）与 Task 07（`%13`）；协调端继续收口 Task 05 gate 与状态文档一致性。
- 2026-05-26 14:32: Task 07 经独立复核通过；期间追加 `use-bridged-search-adapter` 以修复 back/forward 导致的 adapter subscriber 同步缺口。
- 2026-05-26 14:54: Task 06 经二轮 review 后通过；首轮产品实现因重复自写 adapter bridge 被退回，现已统一复用 `use-bridged-search-adapter`，允许进入 Task 08 联调准备。
- 2026-05-26 14:56: 已正式分配 Task 08 给 `%13`；预案确认将通过单点 feature flag、双 project Playwright smoke 与 rollback-only `page-cache` 策略推进。
- 2026-05-26 15:58: 产品约束升级，v1 方向与 `URL 作为 table 状态源 / WorkspaceRouteDefinition / searchAdapter` 假设发生根本冲突；已暂停旧 Task 08 执行。
- 2026-05-26 16:20: 新增 `spec-v2/` 作为替代方案，采用 `轻壳 tags + Activity host + feature-local state`；v1 spec 保留为历史记录，后续执行等待 v2 规格评审通过。
- 2026-05-26 16:42: `%14` 对 v2 首版规格给出 `61/100` 评审，指出单一渲染 owner、V2-02 任务拆分、closeGuard 确定性算法、release gate 自动化与 flag-off 回退目标定义不充分。
- 2026-05-26 17:05: 已根据 `%14` 评审回写 `spec-v2/`，补充 host ownership contract、最小 interface sketch、V2-02A/V2-02B 拆分、closeGuard 算法、release gate matrix 与 v1 retirement criteria；等待二次评审。
- 2026-05-26 17:18: `%14` 对二版规格给出 `87/100` 评审，撤销“不可执行”判断；剩余缺口集中在 `keepAlive=false` owner model、首屏/首开 tag 无空白 gate、legacy 文件 task owner 与 zero-import gate、以及 closeGuard 常量写死。
- 2026-05-26 17:28: 已按二次评审继续回写 `spec-v2/`，补充 `flag-on + keepAlive=false` 的 host owner 模型、无空白 viewport gate、legacy file ownership 与 `rg` 零 import gate、`closeGuardTimeoutMs=1500` 与 batch 顺序来源；等待三次评审。
- 2026-05-26 17:36: `%14` 对三版规格给出 `95/100` 评审；剩余唯一 blocker 为 descriptor / lifecycle 存活期与 route unmount 的契约冲突。
- 2026-05-26 17:46: 已补 route unmatch 仅释放 URL 命中关系、descriptor 注册后转交 shell store 持有、tab close / shell reset 才最终 cleanup 的生命周期合同；等待 `%14` 最终放行评分。
- 2026-05-26 17:50: `%14` 最终复核给出 `100/100`，明确“可按企业级执行计划放行”；`spec-v2/` 现已成为后续执行的唯一有效计划，旧 Task 08 维持 `blocked` 并标记 superseded。
- 2026-05-26: v2 执行正式启动；协调端按 DAG 依赖将 `Task V2-01` 分配给 `%15` 先行实现，`%16` 仅做 `V2-02A / V2-02B` 只读 readiness inventory，不允许在 `V2-01` gate 通过前改动共享 hook 或 feature 代码。
- 2026-05-26 21:52: 协调端独立执行 `Task V2-01` 验证命令，`vitest` 与 `build` 均通过，但 review 未通过：`useWorkspacePage()` 仍依赖 router pathname 推导 `tabId`，`WorkspacePageBoundary` 在 flag-off 下仍会注册 descriptor；`Task V2-01` 退回 `%15` 修正，`Task V2-02A` 继续 blocked。
- 2026-05-27 09:18: 协调端完成 `Task V2-01` 二轮复核。`useWorkspacePage()` 已改为 `ActivityHost` 注入的 store-backed context，flag-off 路径已去除 descriptor/store side effect，`WorkspacePageBoundary` 允许省略 `tabId` 并默认回落 `pathname`；独立验证 `bunx vitest run src/features/workspace-tabs/ src/components/layout/tags-bar.test.tsx` -> PASS（10 files / 162 tests），`bun run build` -> PASS。`Task V2-01` 正式通过并解锁 `Task V2-02A`。
- 2026-05-27 09:25: 协调端完成 `Task V2-02A` 首轮独立验收。`bunx vitest run src/hooks/use-data-table.internal-state.test.tsx src/lib/data-table-page-size.test.ts` -> PASS（2 files / 25 tests），`bun run build` -> PASS；但 review 未通过：`src/hooks/use-data-table.ts` 通过 `if (searchAdapter) return useDataTableWithAdapter(...)` 条件分支调用自定义 hook，存在 `searchAdapter` 跨渲染切换时的 hook-order 风险。`Task V2-02A` 进入 `review`，已要求 `%15` 在原任务文件范围内修正并补回归测试；`Task V2-02B` 继续 blocked。
- 2026-05-27 09:34: 协调端完成 `Task V2-02A` 二轮 review。`%15` 已消除条件 custom hook 调用，并新增 mode-switch 测试，`bunx vitest run src/hooks/use-data-table.internal-state.test.tsx src/hooks/use-data-table.search-adapter.test.tsx src/lib/data-table-page-size.test.ts` -> PASS（3 files / 50 tests）；但仍未放行：当前 compat 实现存在 render-phase `setLocalSearch(...)` 与在 React state updater 内调用 `searchAdapter.setSearch(...)` 两个语义风险。已要求 `%15` 继续在 V2-02A 文件范围内返工，并补“单次交互只触发一次 adapter 更新/订阅通知”的回归测试；`Task V2-02B` 继续 blocked。
- 2026-05-27 09:45: 协调端完成 `Task V2-02A` 三轮独立验收。`bunx vitest run src/hooks/use-data-table.internal-state.test.tsx src/hooks/use-data-table.search-adapter.test.tsx src/lib/data-table-page-size.test.ts` -> PASS（3 files / 54 tests），`bunx vitest run src/features/workspace-tabs/ src/components/layout/tags-bar.test.tsx src/hooks/use-data-table.internal-state.test.tsx src/hooks/use-data-table.search-adapter.test.tsx src/lib/data-table-page-size.test.ts` -> PASS（13 files / 216 tests），`bun run build` -> PASS；但 review 仍未通过：compat 实现改为 `useEffect` 在 commit 后同步新的 adapter snapshot，导致 adapter identity 切换的首个 render 仍消费旧 `localSearch`。这会产生一帧旧 pagination/sort/filter，可能触发错误 query key、多余请求或 UI 闪旧状态。已要求 `%15` 改为 render 期可安全读取当前 adapter snapshot 的方案（建议 `useSyncExternalStore` 或等价实现），并补能真正卡住“首帧不得渲染旧 search”的回归测试；`Task V2-02B` 继续 blocked。
- 2026-05-27 09:51: 协调端完成 `Task V2-02A` 四轮独立验收。`bunx vitest run src/hooks/use-data-table.internal-state.test.tsx src/hooks/use-data-table.search-adapter.test.tsx src/lib/data-table-page-size.test.ts` -> PASS（3 files / 54 tests），`bunx vitest run src/features/workspace-tabs/ src/components/layout/tags-bar.test.tsx src/hooks/use-data-table.internal-state.test.tsx src/hooks/use-data-table.search-adapter.test.tsx src/lib/data-table-page-size.test.ts` -> PASS（13 files / 216 tests），`bun run build` -> PASS；但 review 仍未通过：compat 实现改为 `useSyncExternalStore(subscribe, getSnapshot)` 后，缺少第三个 `getServerSnapshot` 参数，已被协调端用最小 SSR 复现实证为 server-rendered content blocker（`Missing getServerSnapshot, which is required for server-rendered content. Will revert to client rendering.`）。另一个次级问题是 identity-switch 测试仍未直接断言首个 render 的渲染值来自新 adapter。已要求 `%15` 补 SSR-safe `getServerSnapshot` 并增强 identity-switch 测试；`Task V2-02B` 继续 blocked。
- 2026-05-27 10:00: 协调端完成 `Task V2-02A` 五轮独立验收。`bunx vitest run src/hooks/use-data-table.internal-state.test.tsx src/hooks/use-data-table.search-adapter.test.tsx src/lib/data-table-page-size.test.ts` -> PASS（3 files / 54 tests），`bunx vitest run src/features/workspace-tabs/ src/components/layout/tags-bar.test.tsx src/hooks/use-data-table.internal-state.test.tsx src/hooks/use-data-table.search-adapter.test.tsx src/lib/data-table-page-size.test.ts` -> PASS（13 files / 216 tests），`bun run build` -> PASS。compat 路径已补 `useSyncExternalStore` 第三个 `getServerSnapshot` 参数，SSR blocker 关闭；identity-switch 测试已直接断言切换后首个 render 显示 adapterB 的 `page=5 / perPage=100`，首帧旧 snapshot 风险有自动化保护。`Task V2-02A` 正式通过，并已解锁 `%16` 启动 `Task V2-02B`。
- 2026-05-27 10:19: 协调端完成 `Task V2-02B` 首轮独立验收。`bunx vitest run --exclude 'e2e'` -> PASS（18 files / 275 tests），`bunx vitest run src/features/products/components/product-tables.internal-state.test.tsx src/features/users/components/users-table.internal-state.test.tsx` -> PASS（2 files / 17 tests），`bun run build` -> PASS；products/users 主链路也已切到 internal-state + `WorkspacePageBoundary`。但 review 未通过：按任务 acceptance 指定的 `rg -n "product-workspace-definition|users-workspace-definition|use-bridged-search-adapter|searchAdapter|useSearch|WorkspaceRoutePage" src/routes/dashboard/product src/routes/dashboard/users.tsx src/features/products src/features/users` 仍命中非 legacy 文件，包括 `src/features/users/info-content.ts` 的过期 URL-state 说明，以及两个新 internal-state 测试里对 `useSearch` 的 mock。`Task V2-03` 继续 blocked，已要求 `%16` 在 V2-02B 文件范围内完成 gate 收口后再复审。
- 2026-05-27 10:24: 协调端完成 `Task V2-02B` 二轮独立验收。`bunx vitest run src/features/products/components/product-tables.internal-state.test.tsx src/features/users/components/users-table.internal-state.test.tsx` -> PASS（2 files / 25 tests），`bunx vitest run --exclude 'e2e'` -> PASS（18 files / 283 tests），`bun run build` -> PASS；两个 internal-state 测试里的 `useSearch` mock 已清理，过期 URL-state 说明也已改成 V2 internal-state 叙述。但 review 仍未通过：`rg` gate 现在只剩一个非 legacy 命中，即 `src/features/users/info-content.ts` 里字面出现的 `no searchAdapter`。语义虽然正确，但仍违反 `Task V2-02B` acceptance 里对 zero-import / legacy-only gate 的字面约束。已要求 `%16` 仅做最后一处文案改名后再复审；`Task V2-03` 继续 blocked。
- 2026-05-27 10:27: 协调端完成 `Task V2-02B` 三轮独立验收。`bunx vitest run src/features/products/components/product-tables.internal-state.test.tsx src/features/users/components/users-table.internal-state.test.tsx` -> PASS（2 files / 25 tests），`bunx vitest run --exclude 'e2e'` -> PASS（18 files / 283 tests），`bun run build` -> PASS；`rg -n "product-workspace-definition|users-workspace-definition|use-bridged-search-adapter|searchAdapter|useSearch|WorkspaceRoutePage" src/routes/dashboard/product src/routes/dashboard/users.tsx src/features/products src/features/users` 现仅剩 legacy workspace 自引用测试命中，zero-import / legacy-only gate 正式通过。`Task V2-02B` 升级为 `done`，并已解锁 `%15` 启动 `Task V2-03`。
- 2026-05-27 10:38: 协调端完成 `Task V2-03` 首轮独立验收。`bunx vitest run src/features/workspace-tabs/components/workspace-routing.integration.test.tsx src/features/workspace-tabs/hooks/use-workspace-page.test.tsx` -> PASS（2 files / 49 tests），`bunx vitest run --exclude 'e2e'` -> PASS（18 files / 297 tests），`bun run build` -> PASS；但 review 未通过：实现把大量 dashboard 页面统一标为 `keepAlive=false`，包括 `overview/chat/product detail/forms/icons/kanban/react-query/notifications` 等真实页面，违反 `Task V2-03` 对“所有 dashboard 页面默认进入 tags + Activity shell，只有确实不适合保活的页面才允许显式 opt-out 且需记录原因”的约束。当前 route-instance tags 与 closeGuard 行为虽然通过测试，但 keepAlive 语义被大范围退化为 opt-out，`Task V2-04` 继续 blocked，已要求 `%15` 收敛 opt-out 范围并逐页给出必要性说明后再复审。
- 2026-05-27 11:25: 协调端完成 `Task V2-03` 二轮独立验收。`bunx vitest run src/features/workspace-tabs/components/workspace-routing.integration.test.tsx src/features/workspace-tabs/hooks/use-workspace-page.test.tsx` -> PASS（2 files / 49 tests），`bunx vitest run --exclude 'e2e'` -> PASS（18 files / 297 tests），`bun run build` -> PASS；route 文件中的 `keepAlive=false` 已收敛为三处允许例外：`/dashboard/` 重定向路由、`/dashboard/forms/` 重定向路由、`/dashboard/product/$productId` 路由实例页。其余 dashboard 真实页面已恢复默认 keep-alive，route-instance tags 与 closeGuard 自动化保护未回退。`Task V2-03` 正式通过，并已解锁 `%15` 启动 `Task V2-04`。
- 2026-05-27 11:39: 协调端完成 `Task V2-04` 首轮独立复审。`bun run lint` -> PASS（0 errors / 30 warnings），`bunx vitest run` -> PASS（18 files / 297 tests），`bun run build` -> PASS；但 review 未通过：1) `src/routes/dashboard/product/index.tsx` 的 flag-off 路径仍返回 `ProductPageLegacy`，继续依赖 `PageCacheProvider` 与 `ProductPageCacheBindings`，违反 `Task V2-04` 对“flag-off 不得复活 v1 route-state / adapter / definition，page-cache 仅做 inventory”的约束；2) `e2e/workspace-tabs-smoke.spec.ts` 的 `reload reset` 与 `dirty close guard` 场景断言仍然偏弱，没有真正证明“先修改状态再 reload 后回默认”以及“dirty page 的 closeGuard 拒绝会中止关闭并保留焦点”；3) task 指定的 `rg` gate 仍命中未标记 deprecated / inventory-only 的文件，包括 `src/features/workspace-tabs/hooks/use-bridged-search-adapter.ts`、`src/features/workspace-tabs/types.ts` 与若干相关测试文件。Playwright gate 本轮未独立执行，因为在现有 code/spec blockers 未关闭前没有继续投入双 dev server 启动验证。已要求 `%15` 收口这三项后再回审。
- 2026-05-27 12:02: 协调端完成 `Task V2-04` 二轮独立复审。代码侧已有进展：`src/routes/dashboard/product/index.tsx` 已改为 flag-off 直接渲染 `ProductWorkspaceScreen`，`page-cache` 回流 blocker 已关闭；`rg` 命中也已收敛到显式 `deprecated / inventory-only` 文件或 `types.ts` 中明确标记的 V1 legacy type section。fresh 验证：`bun run lint` -> PASS（0 errors / 30 warnings），`bunx vitest run` -> PASS（18 files / 297 tests），`bun run build` -> PASS，`bunx playwright test e2e/workspace-tabs-smoke.spec.ts --project default` -> FAIL（10 failed / 1 passed），`bunx playwright test e2e/workspace-tabs-smoke.spec.ts --project rollback` -> FAIL（2 failed / 1 passed），`bunx playwright test e2e/workspace-tabs-smoke.spec.ts --grep "@workspace-v2"` -> FAIL（`Running 14 tests using 2 workers`，错误地把 rollback cases 一并选入），`VITE_ENABLE_WORKSPACE_TABS=0 bunx playwright test e2e/workspace-tabs-smoke.spec.ts --grep "@workspace-v2-rollback"` -> FAIL（`default` 与 `rollback` projects 同时入选）。当前新的 blockers 是：1) `playwright.config.ts` 的 `default` project 使用 `grep: /@workspace-v2/`，会误选 `@workspace-v2-rollback`；2) smoke 断言与真实 DOM 契约不匹配，多个测试假设 `Products/Users` heading 或立即可见的表格首行，但对应页面组件并未渲染这些 heading，导致 default/rollback 两条路径均有真实失败；3) 浏览器 smoke 仍未覆盖真正的 dirty closeGuard rejection 场景，只验证了非 dirty close plumbing。已要求 `%15` 按新 findings 收口后再回审。
- 2026-05-27 14:57: 协调端在 fresh default smoke 中复现到“第 7 个用例后 `ERR_CONNECTION_REFUSED`”。根因定位为 `scripts/playwright-workspace-tabs-servers.sh` 使用 `wait -n` 绑定 default/rollback 双 server 生命周期；任一子进程退出都会触发 wrapper 结束并由 trap 一并杀掉另一个 project 的 server，造成 default 线被 rollback 线误伤。
- 2026-05-27 15:13: 协调端完成 rollback SSR root-cause 调查。`/tmp/playwright-workspace-rollback.log` 明确报 `No QueryClient set, use QueryClientProvider to set one`，调用栈落在 `UserFormSheet -> useMutation`。对比 flag-on / flag-off 链路后确认：`src/routes/dashboard.tsx` 只把 `QueryClientProvider` 包在 `WorkspaceViewport` 外，flag-off 时真实页面经 `Outlet` 直渲染，导致 products/users rollback 路径脱离 React Query context。
- 2026-05-27 15:14: 协调端完成 `Task V2-04` 最终收口。修复包括：1) `scripts/playwright-workspace-tabs-servers.sh` 改为等待两个 server 常驻，解除 cross-project teardown；2) `src/routes/dashboard.tsx` 将 `QueryClientProvider` 上提到 `WorkspaceViewport + Outlet` 外层，统一 flag-on / flag-off provider 链；3) `e2e/workspace-tabs-smoke.spec.ts` 收紧 close-other/rollback smoke 断言。fresh 验证：`bun run lint` -> PASS（0 errors / 30 warnings），`bunx vitest run` -> PASS（18 files / 297 tests），`bun run build` -> PASS，`bunx playwright test e2e/workspace-tabs-smoke.spec.ts --project default` -> PASS（10 passed），`bunx playwright test e2e/workspace-tabs-smoke.spec.ts --project rollback` -> PASS（3 passed），`bunx playwright test e2e/workspace-tabs-smoke.spec.ts` -> PASS（13 passed）。`Task V2-04` 正式标记为 `done`，v2 rollout / regression gate 关闭。
- 2026-05-27 15:18: 协调端在最终产品语义审计中重新打开一个 P1 残余缺口：`product/$productId` 仍显式 `keepAlive=false`，但其页面承载 `ProductForm` 的 `dirty / closeGuard`。结果是用户切走 tab 后草稿实例已卸载，`close-other / close-all` smoke 虽能回到原 URL，却没有证明 draft 仍在。已将代码修复分派给 `%15`，并让 `%16` 独立复核所有 `keepAlive=false` opt-out 与文档结论。
- 2026-05-27 15:32: 该缺口已收口：`src/routes/dashboard/product/$productId.tsx` 回归默认 keep-alive，`ProductForm` 的 dirty closeGuard 现在会给出页面侧 warning toast，`e2e/workspace-tabs-smoke.spec.ts` 新增/收紧了“tag 切换后 draft 仍在”“close guard 拒绝后 draft 仍在”的断言，并把多实例表单 selector 收敛到当前活动实例。fresh 验证：`bunx vitest run` -> PASS（18 files / 297 tests），`bun run lint` -> PASS（0 errors / 28 warnings），`bun run build` -> PASS，`bunx playwright test e2e/workspace-tabs-smoke.spec.ts --project default` -> PASS（11 passed），`bunx playwright test e2e/workspace-tabs-smoke.spec.ts --project rollback` -> PASS（3 passed）。`Task V2-03` 的唯一残余风险已关闭，v2 状态维持 `done`。
