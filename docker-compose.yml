services:
  tanstack-admin:
    build: .
    container_name: tanstack-admin
    restart: always
    ports:
      - '127.0.0.1:3000:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
